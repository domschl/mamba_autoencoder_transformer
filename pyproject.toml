[project]
name = "arnold-transformer"
version = "0.1.0"
description = "Transformer from scratch as test-harness for the Arnold Activation Function"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.1",
    "torch>=2.9.1",
    "tiktoken>=0.8.0",
]
